export enum DeviceTypes {
  IPC = 'Camera',
  Socket = 'Plug',
  CatEye = 'Peephole',
}

export enum SwitchTypes {
  On = 14,
  Sleep = 21,
  Audio = 22,
}

export enum DefenceMode {
  UNSET_MODE = 0,
  HOME_MODE = 1,
  AWAY_MODE = 2,
  SLEEP_MODE = 3,
}
