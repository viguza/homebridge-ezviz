## What this changes

<!-- What does this PR do, and why? Link the issue it fixes, e.g. "Fixes #123". -->

## How it was tested

<!-- Tests added or updated, and any testing against real devices (model, firmware). -->

## Checklist

- [ ] `npm run lint`, `npm run build` and `npm test` pass
- [ ] Tests added or updated for the changed behavior
- [ ] `CHANGELOG.md` updated under `[Unreleased]`
- [ ] No secrets in code, tests, logs or screenshots (passwords, verification codes, session IDs)
- [ ] README updated if configuration or user-visible behavior changed
